{
  "name": "mines-robot",
  "version": "0.1.0",
  "description": "robot player for mines npm module",
  "main": "./dist/index.js",
  "scripts": {
    "dist": "babel src --presets babel-preset-es2015 --ignore '**/*-spec.js' --out-dir dist",
    "test": "mocha --compilers js:babel-core/register 'src/**/*-spec.*' && eslint src"
  },
  "keywords": [
    "mines",
    "minesweeper",
    "game"
  ],
  "author": "Mark Ryall <mark@ryall.name> (http://mark.ryall.name)",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.10.1",
    "babel-core": "^6.10.4",
    "babel-eslint": "^6.1.0",
    "babel-preset-es2015": "^6.9.0",
    "eslint": "^2.13.1",
    "expect": "^1.20.1",
    "mocha": "^2.5.3"
  },
  "dependencies": {
    "lodash": "^4.13.1",
    "mines": "^1.0.2"
  }
}
